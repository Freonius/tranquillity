from setuptools import setup, find_packages

setup(name='tranquillity.shell', version='0.1.0', packages=find_packages(exclude=['test', ],), install_requires=[],
      author='Federico Pirani',
      description='Tranquillity shell utils.',)
