from setuptools import setup, find_packages

setup(name='tranquillity.data', version='0.1.0', packages=find_packages(), install_requires=[],
      author='Federico Pirani',
      description='Tranquillity data.')
